[package]
name = "nicti-ai"
version = "0.0.0"
edition = "2021"
publish = false
description = "AI model provider extension point (ADR-0004 §8). Crate boundary only — execution and the ort/load-dynamic ONNX runtime boundary (ADR-0004 §3) are owned by the AI-labeled tickets (#48-#53, #35, #34, #33, #36)."

[dependencies]
nicti-claw = { path = "../nicti-claw" }

[dev-dependencies]
serde_json = "1"
