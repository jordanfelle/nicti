//! Benchmark + golden-image harness (#17). See `docs/benchmarks.md` for the methodology this
//! crate implements, and each module's own docs for the specific piece.

pub mod golden;
pub mod manifest;
pub mod perf;
pub mod refset;
